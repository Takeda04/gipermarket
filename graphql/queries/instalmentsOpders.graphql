query InstalmetsOrder($id:ID!,$first:Int!){
  contracts(user:$id,first:$first){
    edges {
      node{
        products
        status
        paymentDate
        createdAt
        limit
        totalPrice
        totalCount
        
      }
    }
  }
}